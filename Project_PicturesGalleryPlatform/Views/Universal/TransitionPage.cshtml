@{
    // if (TempData["triggerAlert"] != null && (bool)TempData["triggerAlert"])
    if (true)
    {
        <script type="text/javascript">
            var feedbackMessage = @Html.Raw(Json.Serialize(TempData["feedbackMessage"]));
            alert(feedbackMessage);
            window.location.href = '@Url.Action(TempData["action"] + "", TempData["controller"] + "")';
        </script>
    }
}
